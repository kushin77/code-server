.PHONY: help test lint fmt build

help:
	@echo "Available targets: test lint fmt build"

test:
	@echo "No tests defined yet. Add unit tests and update Makefile."

lint:
	@echo "No linters configured. Add linter commands."

fmt:
	@echo "Formatting not configured. Add formatters (eg. terraform fmt)."

build:
	@echo "No build steps defined. Add build instructions."
