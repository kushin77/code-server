apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: agents
  labels:
    app: agent-api
spec:
  type: ClusterIP
  selector:
    app: agent-api
  ports:
    - port: 8000
      targetPort: 8000
