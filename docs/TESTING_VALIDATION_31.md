# Testing & Validation Specification for 192.168.168.31## Smoke Tests, Performance Benchmarks, Failover Scenarios**Version**: 1.0**Date**: April 13, 2026**Scope**: Deployment validation, performance baselines, SLA verification**Target SLOs**: GPU 99.99%, NAS 99.95%, App latency <500ms p99---## 1. Deployment Validation Test Suite### PurposeValidate that deployment to 192.168.168.31 is complete and all components are operational.### Test Categories#### Service Health Checks (6 tests)- **Docker Running**: `docker ps` succeeds- **Docker Compose Available**: `docker-compose --version` or `docker compose version`- **Services Running**: code-server, ollama, prometheus all running- **Code-Server Responsive**: HTTP 200 on http://localhost:8443- **Ollama Responsive**: HTTP 200 on http://localhost:11434/api/tags- **Prometheus Healthy**: HTTP 200 on http://localhost:9090/-/healthy#### GPU Validation (5 tests)- **GPU Available**: `nvidia-smi` lists 1+ GPUs- **CUDA Toolkit**: `nvcc --version` shows CUDA 12.4+- **cuDNN Library**: libcudnn.so found in standard locations- **GPU Memory**: Total GPU RAM reported (e.g., 40GB per GPU)- **Docker GPU Access**: `docker run --gpus all nvidia/cuda:12.4-base nvidia-smi` succeeds#### NAS/Storage (4 tests)- **Mount Points**: `/mnt/nas-primary` and `/mnt/nas-backup` are mounted- **Writable**: Can create/delete test file on primary NAS- **Capacity**: >500GB free on primary NAS- **Latency**: p99 latency <100ms on NAS I/O (optional, uses iozone)#### Network (4 tests)- **DNS Resolution**: `nslookup google.com` succeeds- **Internet Connectivity**: `curl https://api.github.com` succeeds- **SSH Access**: SSH to localhost works- **Firewall Rules**: Ports 8443, 11434, 9090, 3000, 9100 accessible#### Docker Images (2 tests)- **Code-Server Image**: Present in docker images list- **Ollama Image**: Present in docker images list#### Configuration (1 test)- **Environment Variables**: Required vars set (OLLAMA_NUM_GPU, CODE_SERVER_HOST)#### Security (2 tests)- **SSH Key-Auth Only**: PasswordAuthentication disabled in sshd_config- **No Hardcoded Secrets**: No password/API key/secret in config files#### Performance Baseline (2 tests)- **CPU Benchmark**: Run sysbench CPU test, measure operations/sec- **Memory Benchmark**: Run sysbench memory test, measure throughput**Execution**: `./scripts/deployment-validation-31.sh`**Expected Output**:```✓ Docker is running✓ Docker Compose is available✓ Service 'code-server' is running✓ Code-Server is responsive on :8443✓ Ollama API is responsive on :11434✓ Prometheus is healthy on :9090✓ GPU detected: 2 GPU(s) available✓ CUDA Toolkit available: version 12.4✓ cuDNN library found at /usr/local/cuda/lib64/libcudnn.so✓ GPU memory available: 40960 MB✓ Docker GPU access is working✓ NAS mount point is healthy: /mnt/nas-primary✓ NAS mount point is healthy: /mnt/nas-backup✓ NAS mount point is writable✓ NAS has sufficient capacity: 2048GB free...═══════════════════════════════════════════════════════════════Test Summary:  Passed:  27  Failed:  0  Skipped: 2═══════════════════════════════════════════════════════════════✓ VALIDATION PASSED```**Pass Criteria**: All critical tests pass, failures = deployment not ready---## 2. Performance Benchmarking### GPU Performance Benchmarks#### Ollama Model Inference Latency**Test**: Load llama2:70b-chat and measure inference latency```bash# Warm up (2 inferences)curl http://localhost:11434/api/generate -d '{  "model": "llama2:70b-chat",  "prompt": "What is machine learning?",  "stream": false}' | jq '.total_duration / 1e9'# Measure 10 inferences, record timingsfor i in {1..10}; do  curl http://localhost:11434/api/generate -d '{"model":"llama2:70b-chat","prompt":"Hello world","stream":false}' | jq '.total_duration / 1e9'done```**Metrics**:- p50 latency (median)- p99 latency (99th percentile)- Token generation rate (tokens/sec)- VRAM utilization during inference**SLA Target**: p99 <500ms per inference#### GPU Utilization Under Load**Test**: Sustained 10 concurrent Ollama inferences```bash# Monitor GPU utilization during testwatch -n 1 nvidia-smi --query-gpu=utilization.gpu,utilization.memory --format=csv,noheader```**Metrics**:- GPU utilization % (target: 80-95%)- GPU memory usage % (target: 80-90%)- Temperature (target: <80°C)- Power consumption (W)### NAS Performance Benchmarks#### Throughput Test**Test**: Sequential read/write across NAS```bash# 1GB sequential readdd if=/mnt/nas-primary/test-1gb.dat of=/dev/null bs=1M# 1GB sequential writedd if=/dev/zero of=/mnt/nas-primary/test-write.dat bs=1M count=1024```**SLA Target**: 100-200 MB/s throughput#### IOPS Test**Test**: Random read/write patterns```bash# Using fio (if available)fio --name=random-read --ioengine=libaio --iodepth=32 --rw=randread \  --bs=4k --direct=1 --size=10G --filename=/mnt/nas-primary/fio-test --numjobs=4```**SLA Target**: >20k IOPS for random reads#### Latency Test**Test**: Measure I/O operation latency```bash# Using sysbench random I/Osysbench fileio --file-total-size=10G --file-test-mode=rndrd preparesysbench fileio --file-total-size=10G --file-test-mode=rndrd --time=60 run```**SLA Target**: p99 <50ms latency### System Performance Baselines#### CPU Performance**Test**: CPU-intensive workload```bashsysbench cpu --cpu-total-threads=32 --time=60 run```**Baseline Metrics**:- Operations per second- Total execution time- CPU utilization#### Memory Performance**Test**: Memory bandwidth and latency```bashsysbench memory --memory-total-size=16G --time=60 run```**Baseline Metrics**:- Memory operations/sec- Total bytes transferred- Latency metrics#### Network Performance**Test**: Network throughput between systems```bash# Server: iperf3 -s# Client: iperf3 -c 192.168.168.31 -P 4 -t 60```**Baseline Metrics**:- Throughput (Gbps)- Jitter- Packet loss---## 3. Failover & Resilience Testing### GPU Failover Scenarios#### Scenario 1: GPU Driver Crash**Test Procedure**:1. Run continuous Ollama inference workload2. Simulate GPU driver failure: `echo 1 > /proc/sys/kernel/sysrq-b` (warning: will reboot)3. Monitor recovery: Should auto-restart GPU consumer in <2 min**Success Criteria**:- GPU workloads resume automatically- No data loss- Alert triggered for GPU unavailability#### Scenario 2: GPU Out of Memory**Test Procedure**:1. Start increasing batch size in Ollama requests2. Keep adding requests until VRAM exhausted3. Observe behavior**Success Criteria**:- Clear error message returned- No container crash- Graceful degradation to CPU (if configured)#### Scenario 3: GPU Temperature Warning**Test Procedure**:1. Monitor GPU temperature during sustained load2. Trigger thermal throttling by controlled load increase**Success Criteria**:- Alert fires when temp >85°C- Throttling detected in nvidia-smi- Load reduction recommended### NAS Failover Scenarios#### Scenario 1: NAS Network Disconnect**Test Procedure**:1. Simulate NAS network failure: `sudo ifconfig eth0 down` (primary NAS network)2. Observe failover behavior3. Restore network: `sudo ifconfig eth0 up`**Success Criteria**:- Failover to secondary NAS within 30 seconds (RTO <30s)- Data consistency maintained- No data loss (RPO <1 hour)- Alert triggered immediately**Recovery**:```bash# Check mount statusdf /mnt/nas-primary# Remount if neededsudo umount /mnt/nas-primarysudo mount /mnt/nas-primary```#### Scenario 2: NAS Capacity Exhaustion**Test Procedure**:1. Write large files until NAS 90% full2. Observe alert behavior3. Clean up test files**Success Criteria**:- Alert fires at 90% capacity- Cleanup procedures documented- Clear action items provided#### Scenario 3: NAS Latency Degradation**Test Procedure**:1. Start high I/O workload on NAS2. Monitor latency metrics via Prometheus3. Scale down workload**Success Criteria**:- p99 latency tracked in metrics- Alert fires if >100ms p99- Workload recommendation provided### Application Failover Scenarios#### Scenario 1: Code-Server Service Crash**Test Procedure**:1. Kill code-server container: `docker kill <container_id>`2. Observe recovery**Success Criteria**:- Container automatic restart (Docker restart policy)- Alert triggered- Service up within <30 seconds**Recovery**:```bashdocker restart code-servercurl http://localhost:8443```#### Scenario 2: Ollama Model Load Failure**Test Procedure**:1. Request non-existent model: `ollama pull fake-model:latest`2. Monitor error handling**Success Criteria**:- Clear error message- Alert triggered- No cascading failures---## 4. Data Integrity Tests### Write-Read Verification```bash# Write test dataecho "test-data-$(date +%s)" > /mnt/nas-primary/test.txt# Read and verifycat /mnt/nas-primary/test.txt# Checksum verificationmd5sum /mnt/nas-primary/test.txt > test.md5md5sum -c test.md5```**Success Criteria**: Checksums match### Large File Transfer```bash# Create 1GB test filedd if=/dev/zero of=/tmp/test-1gb.dat bs=1M count=1024# Copy to NAScp /tmp/test-1gb.dat /mnt/nas-primary/# Verify integritycmp -l /tmp/test-1gb.dat /mnt/nas-primary/test-1gb.dat || echo "MISMATCH"```**Success Criteria**: No byte mismatches---## 5. Test Execution Schedule| Test Type | Frequency | Window | Responsible ||-----------|-----------|--------|-------------|| Smoke Tests | Pre-deployment | 5 min | CI/CD || Deployment Validation | Post-deployment | 30 min | DevOps || Performance Baseline | Weekly | 2 hours | Engineering || Failover Scenarios | Monthly | 4 hours | Engineering || Data Integrity | Monthly | 1 hour | DevOps || Full Validation Suite | Quarterly | 8 hours | Engineering |---## 6. Reporting### Performance Baseline Report```Deployment Date: April 13, 2026Host: 192.168.168.31GPU Metrics:- Ollama p99 latency: 485ms (target: <500ms) ✓- Token rate: 52 tokens/sec (target: 50+) ✓- Model load time: 8.2s (target: <10s) ✓- GPU memory utilization: 88% (target: 80-90%) ✓NAS Metrics:- Throughput: 185 MB/s (target: 100-200) ✓- Latency p99: 35ms (target: <50ms) ✓- IOPS: 24.5k (target: >20k) ✓System Metrics:- CPU available: 64 cores- Memory available: 256GB- Disk space: 2TBSLA Status: ALL METRICS WITHIN TARGET ✓```---## Next Steps1. Execute deployment validation test suite within 1 hour of deployment2. Run performance benchmarks daily for first week3. Establish baseline metrics for SLA monitoring4. Schedule monthly failover tests5. Update thresholds based on observed behavior
