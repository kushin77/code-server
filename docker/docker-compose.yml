# ╔══════════════════════════════════════════════════════════════════════════╗
# ║  RETIRED — DO NOT USE                                                   ║
# ║  This file has been superseded by the canonical compose baseline.       ║
# ║  Canonical file: /docker-compose.yml (repo root)                        ║
# ║                                                                          ║
# ║  REASON: Weak password fallback (PASSWORD:-change-me-in-env), local     ║
# ║  volumes (not NFS-backed), stale image pins, not aligned to NAS .56.   ║
# ║  See issue #730 for retirement rationale.                               ║
# ╚══════════════════════════════════════════════════════════════════════════╝
#
# To deploy: ssh akushnir@192.168.168.31
#            cd ~/code-server-enterprise
#            docker compose up -d
#
# This file is intentionally left as a comment-only stub to prevent
# accidental use of this retired compose file.