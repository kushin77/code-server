# Optional override for local-only container development workflows.
# Keep this out of standard production redeploy paths.

version: "3.9"

services:
  session-broker:
    environment:
      - DOCKER_SOCKET=unix:///var/run/docker.sock
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
