apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: agent-farm-catalog
  description: Agent Farm software catalog
spec:
  targets:
    - ./apis.yaml
    - ./components.yaml
    - ./groups.yaml
    - ./systems.yaml
