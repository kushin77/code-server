# ============================================================================
# .env.base - BASE CONFIGURATION DEFAULTS (SSOT Foundation)
# All application and infrastructure configuration variables
# 
# CONSOLIDATION STRATEGY (Phase 3 - Environment Variables):
# - .env.base = Base defaults for all environments (this file)
# - .env.consolidated = Merged view (generated from above + overrides)
# - .env.infrastructure = Infrastructure-specific overrides
# - .env.deployment = Deployment-specific overrides
# - .env.cluster = Cluster networking overrides
# - .env.production = Production secrets and overrides
#
# LOAD ORDER (priority, highest to lowest):
#   1. .env.base (this file - defaults for all environments)
#   2. .env.infrastructure (infrastructure URLs and endpoints)
#   3. .env.deployment (deployment-specific settings)
#   4. .env.cluster (cluster networking and HA settings)
#   5. .env.production (production overrides and secrets)
#
# USAGE: Scripts should source variables in this order:
#   source .env.base
#   [ -f .env.infrastructure ] && source .env.infrastructure
#   [ -f .env.deployment ] && source .env.deployment
#   [ -f .env.cluster ] && source .env.cluster
#   [ -f .env.production ] && source .env.production
#
# Terraform consolidation: See terraform/environments/_common/README.md
# Date: April 30, 2026
# ============================================================================

# ============================================================================
# SECTION 1: CORE DOMAIN & IDENTITY CONFIGURATION (SSOT)
# ============================================================================
# Primary domain for all services (SOURCE OF TRUTH)
APEX_DOMAIN=${APEX_DOMAIN:-kushnir.cloud}

# Domain aliases for specific services (derived from APEX_DOMAIN)
AUTH_DOMAIN=${AUTH_DOMAIN:-auth.${APEX_DOMAIN}}
APPSMITH_DOMAIN=${APPSMITH_DOMAIN:-${APEX_DOMAIN}}
CODE_SERVER_DOMAIN=${CODE_SERVER_DOMAIN:-${APEX_DOMAIN}}
IDE_DOMAIN=${IDE_DOMAIN:-ide.${APEX_DOMAIN}}
API_DOMAIN=${API_DOMAIN:-api.${APEX_DOMAIN}}
REGISTRY_DOMAIN=${REGISTRY_DOMAIN:-registry.${APEX_DOMAIN}}

# TLS/Certificate configuration
TLS_EMAIL=${TLS_EMAIL:-admin@${APEX_DOMAIN}}
ADMIN_EMAIL=${ADMIN_EMAIL:-admin@kushnir.cloud}

# ============================================================================
# SECTION 2: API & PROTOCOL CONFIGURATION (SSOT)
# ============================================================================
API_PROTOCOL=${API_PROTOCOL:-http}
API_HOST=${API_HOST:-192.168.168.31}
API_PORT=${API_PORT:-8080}
API_ENDPOINT=${API_ENDPOINT:-${API_PROTOCOL}://${API_HOST}:${API_PORT}}
API_HEALTH_ENDPOINT=${API_HEALTH_ENDPOINT:-${API_ENDPOINT}/health}
API_OAUTH_CALLBACK=${API_OAUTH_CALLBACK:-${API_ENDPOINT}/oauth/callback}

# ============================================================================
# SECTION 3: CLUSTER & HIGH AVAILABILITY CONFIGURATION (SSOT)
# ============================================================================
CLUSTER_VIP=${CLUSTER_VIP:-192.168.168.250}
CLUSTER_HOST_1=${CLUSTER_HOST_1:-192.168.168.31}
CLUSTER_HOST_2=${CLUSTER_HOST_2:-192.168.168.42}
PRIMARY_HOST=${PRIMARY_HOST:-192.168.168.31}
REPLICA_HOST=${REPLICA_HOST:-192.168.168.42}

# Deployment mode: primary, replica, or private
DEPLOYMENT_MODE=${DEPLOYMENT_MODE:-private}
REPLICA_ENABLED=${REPLICA_ENABLED:-true}
REPLICATION_MODE=${REPLICATION_MODE:-primary}

# ============================================================================
# SECTION 4: DATABASE CONFIGURATION (SSOT)
# ============================================================================
# PostgreSQL primary configuration
DATABASE_HOST=${DATABASE_HOST:-code-server-postgres}
DATABASE_PORT=${DATABASE_PORT:-5432}
POSTGRES_PORT=${POSTGRES_PORT:-5432}
DB_USER=${DB_USER:-postgres}
DB_PASSWORD=${DB_PASSWORD:-postgres-dev-password}
DB_NAME=${DB_NAME:-code_server}
POSTGRES_DB=${POSTGRES_DB:-kushnir_db}
POSTGRES_USER=${POSTGRES_USER:-postgres}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres-dev-password}
POSTGRES_INITDB_ARGS=${POSTGRES_INITDB_ARGS:--c max_wal_senders=10 -c wal_level=replica}

# Database connection URL (constructed)
DATABASE_URL=${DATABASE_URL:-postgresql://${DB_USER}:${DB_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DB_NAME}}

# Connection pooling
DATABASE_POOL_SIZE=${DATABASE_POOL_SIZE:-20}
DATABASE_MAX_OVERFLOW=${DATABASE_MAX_OVERFLOW:-40}

# ============================================================================
# SECTION 5: REDIS CONFIGURATION (SSOT)
# ============================================================================
REDIS_HOST=${REDIS_HOST:-code-server-redis}
REDIS_PORT=${REDIS_PORT:-6379}
REDIS_PASSWORD=${REDIS_PASSWORD:-redis-dev-password}
REDIS_MAX_MEMORY=${REDIS_MAX_MEMORY:-512mb}
REDIS_MAXMEMORY=${REDIS_MAXMEMORY:-6gb}
REDIS_EVICTION_POLICY=${REDIS_EVICTION_POLICY:-allkeys-lru}
REDIS_MAXMEMORY_POLICY=${REDIS_MAXMEMORY_POLICY:-allkeys-lru}
REDIS_APPENDONLY=${REDIS_APPENDONLY:-yes}

# ============================================================================
# SECTION 6: MESSAGE BROKER (REDPANDA/KAFKA) CONFIGURATION
# ============================================================================
# [SSOT] Redundant: KAFKA_BROKER=${KAFKA_BROKER:-code-server-redpanda:9092}
# [SSOT] Redundant: KAFKA_TOPIC_PREFIX=${KAFKA_TOPIC_PREFIX:-code-server.}
# [SSOT] Redundant: KAFKA_JMX_EXPORTER=${KAFKA_JMX_EXPORTER:-localhost:9308}
# [SSOT] Redundant: REDPANDA_PORT=${REDPANDA_PORT:-9092}
# [SSOT] Redundant: REDPANDA_REST_PORT=${REDPANDA_REST_PORT:-8082}
# [SSOT] Redundant: REDPANDA_SCHEMA_PORT=${REDPANDA_SCHEMA_PORT:-8081}
# [SSOT] Redundant: REDPANDA_BROKERS=${REDPANDA_BROKERS:-code-server-redpanda:9092}
# [SSOT] Redundant: REDPANDA_PARTITIONS=${REDPANDA_PARTITIONS:-3}
# [SSOT] Redundant: REDPANDA_REPLICATION_FACTOR=${REDPANDA_REPLICATION_FACTOR:-2}

# ============================================================================
# SECTION 7: OBSERVABILITY & MONITORING PORTS
# ============================================================================
# [SSOT] Redundant: PROMETHEUS_PORT=${PROMETHEUS_PORT:-9090}
# [SSOT] Redundant: PROMETHEUS_SELF_METRICS_PORT=${PROMETHEUS_SELF_METRICS_PORT:-9200}
# [SSOT] Redundant: PROMETHEUS_RETENTION=${PROMETHEUS_RETENTION:-30d}
# [SSOT] Redundant: PROMETHEUS_SCRAPE_INTERVAL=${PROMETHEUS_SCRAPE_INTERVAL:-15s}
# [SSOT] Redundant: PROMETHEUS_ENDPOINT=${PROMETHEUS_ENDPOINT:-http://localhost:9090}

# [SSOT] Redundant: GRAFANA_PORT=${GRAFANA_PORT:-3000}
# [SSOT] Redundant: GRAFANA_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
# [SSOT] Redundant: GRAFANA_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin-dev-password-12345}
# [SSOT] Redundant: GRAFANA_PASSWORD=${GRAFANA_PASSWORD:-dev-grafana-password-12345}
# [SSOT] Redundant: GRAFANA_SECURITY_ADMIN_USER=${GRAFANA_SECURITY_ADMIN_USER:-admin}
# [SSOT] Redundant: GRAFANA_SECURITY_ADMIN_PASSWORD=${GRAFANA_SECURITY_ADMIN_PASSWORD:-admin-dev-password-12345}
# [SSOT] Redundant: GRAFANA_ENDPOINT=${GRAFANA_ENDPOINT:-http://localhost:3000}

# [SSOT] Redundant: LOKI_PORT=${LOKI_PORT:-3100}
# [SSOT] Redundant: LOKI_RETENTION_DAYS=${LOKI_RETENTION_DAYS:-30}
# [SSOT] Redundant: LOKI_ENABLED=${LOKI_ENABLED:-true}

# [SSOT] Redundant: ALERTMANAGER_PORT=${ALERTMANAGER_PORT:-9093}
# [SSOT] Redundant: ALERTMANAGER_SMTP_FROM=${ALERTMANAGER_SMTP_FROM:-alerts@kushnir.cloud}
# [SSOT] Redundant: ALERTMANAGER_SLACK_URL=${ALERTMANAGER_SLACK_URL:-https://hooks.slack.com/services/dev/dev/dev}

# ============================================================================
# SECTION 8: OPENTELEMETRY (OTEL) & TRACING CONFIGURATION
# ============================================================================
# [SSOT] Redundant: OTEL_EXPORTER_OTLP_GRPC_PORT=${OTEL_EXPORTER_OTLP_GRPC_PORT:-4317}
# [SSOT] Redundant: OTEL_EXPORTER_OTLP_HTTP_PORT=${OTEL_EXPORTER_OTLP_HTTP_PORT:-4318}
# [SSOT] Redundant: OTEL_COLLECTOR_HOST=${OTEL_COLLECTOR_HOST:-code-server-otel-collector}
# [SSOT] Redundant: OTEL_EXPORTER_OTLP_ENDPOINT=${OTEL_EXPORTER_OTLP_ENDPOINT:-http://code-server-otel-collector:4317}

# [SSOT] Redundant: TEMPO_GRPC_PORT=${TEMPO_GRPC_PORT:-4317}
# [SSOT] Redundant: TEMPO_HTTP_PORT=${TEMPO_HTTP_PORT:-3200}
# [SSOT] Redundant: TEMPO_ENABLED=${TEMPO_ENABLED:-true}

# [SSOT] Redundant: JAEGER_ENABLED=${JAEGER_ENABLED:-true}

# ============================================================================
# SECTION 9: AI/ML & VECTOR DATABASE CONFIGURATION
# ============================================================================
# [SSOT] Redundant: OLLAMA_URL=${OLLAMA_URL:-http://code-server-ollama:11434}
# [SSOT] Redundant: OLLAMA_MODEL=${OLLAMA_MODEL:-mistral}
# [SSOT] Redundant: OLLAMA_VISION_MODEL=${OLLAMA_VISION_MODEL:-llava}
# [SSOT] Redundant: OLLAMA_NUM_GPU=${OLLAMA_NUM_GPU:-0}

# [SSOT] Redundant: QDRANT_HOST=${QDRANT_HOST:-code-server-qdrant}
# [SSOT] Redundant: QDRANT_PORT=${QDRANT_PORT:-6333}
# [SSOT] Redundant: QDRANT_GRPC_PORT=${QDRANT_GRPC_PORT:-6334}
# [SSOT] Redundant: QDRANT_API_KEY=${QDRANT_API_KEY:-qdrant-dev-key-12345}

# LLM Backend configuration
# [SSOT] Redundant: DIAGRAM_LLM_BACKEND=${DIAGRAM_LLM_BACKEND:-openai}
# [SSOT] Redundant: VISION_BACKEND=${VISION_BACKEND:-ollama}
# [SSOT] Redundant: TTS_BACKEND=${TTS_BACKEND:-local}
# [SSOT] Redundant: WHISPER_MODEL=${WHISPER_MODEL:-base}

# External LLM integration
# [SSOT] Redundant: OPENAI_API_KEY=${OPENAI_API_KEY:-sk-placeholder-key}

# ============================================================================
# SECTION 10: POLICY ENGINE (OPA) CONFIGURATION
# ============================================================================
# [SSOT] Redundant: OPA_URL=${OPA_URL:-http://code-server-opa:8181}
# [SSOT] Redundant: OPA_ENDPOINT=${OPA_ENDPOINT:-http://localhost:8181}
# [SSOT] Redundant: OPA_ADMIN_TOKEN=${OPA_ADMIN_TOKEN:-opa_admin_secret_2026}
# [SSOT] Redundant: OPA_API_KEY=${OPA_API_KEY:-dev-opa-key-12345}

# ============================================================================
# SECTION 11: APPLICATION SERVICE PORTS & URLS
# ============================================================================
# [SSOT] Redundant: CONTROL_PLANE_PORT=${CONTROL_PLANE_PORT:-8080}

# [SSOT] Redundant: SCHEDULER_PORT=${SCHEDULER_PORT:-8001}
# [SSOT] Redundant: SCHEDULER_URL=${SCHEDULER_URL:-http://code-server-execution-scheduler:8001}
# [SSOT] Redundant: SCHEDULER_API_KEY=${SCHEDULER_API_KEY:-@HiPd0)pCjCxg3qqg#4gYabA}

# [SSOT] Redundant: REPUTATION_ENGINE_PORT=${REPUTATION_ENGINE_PORT:-8002}
# [SSOT] Redundant: REPUTATION_ENGINE_URL=${REPUTATION_ENGINE_URL:-http://code-server-reputation-engine:8002}
# [SSOT] Redundant: REPUTATION_PORT=${REPUTATION_PORT:-8002}
# [SSOT] Redundant: REPUTATION_URL=${REPUTATION_URL:-http://code-server-reputation-engine:8002}
# [SSOT] Redundant: REPUTATION_ENGINE_THRESHOLD=${REPUTATION_ENGINE_THRESHOLD:-0.7}

# [SSOT] Redundant: MEMORY_ENGINE_PORT=${MEMORY_ENGINE_PORT:-8003}
# [SSOT] Redundant: MEMORY_SERVICE_ENDPOINT=${MEMORY_SERVICE_ENDPOINT:-http://localhost:8000}
# [SSOT] Redundant: MEMORY_SERVICE_API_KEY=${MEMORY_SERVICE_API_KEY:-dev-memory-service-key-12345}
# [SSOT] Redundant: MEMORY_SERVICE_MODEL=${MEMORY_SERVICE_MODEL:-gpt-4-turbo}

# [SSOT] Redundant: MODEL_SERVER_PORT=${MODEL_SERVER_PORT:-8004}

# [SSOT] Redundant: MULTIMODAL_AI_PORT=${MULTIMODAL_AI_PORT:-8005}

# [SSOT] Redundant: RAG_ENGINE_PORT=${RAG_ENGINE_PORT:-8006}

# [SSOT] Redundant: PAPERCLIP_PORT=${PAPERCLIP_PORT:-8007}
# [SSOT] Redundant: PAPERCLIP_URL=${PAPERCLIP_URL:-http://code-server-paperclip:8007}

# ============================================================================
# SECTION 12: AUTONOMOUS AGENTS CONFIGURATION
# ============================================================================
# [SSOT] Redundant: AGENT_CODE_REVIEWER_PORT=${AGENT_CODE_REVIEWER_PORT:-9001}
# [SSOT] Redundant: AGENT_INCIDENT_RESPONDER_PORT=${AGENT_INCIDENT_RESPONDER_PORT:-9002}
# [SSOT] Redundant: AGENT_DOCUMENTATION_WRITER_PORT=${AGENT_DOCUMENTATION_WRITER_PORT:-9003}
# [SSOT] Redundant: AGENT_DOC_WRITER_PORT=${AGENT_DOC_WRITER_PORT:-9003}
# [SSOT] Redundant: AGENT_TEST_GENERATOR_PORT=${AGENT_TEST_GENERATOR_PORT:-9004}
# [SSOT] Redundant: AGENT_RUNTIME_PORT=${AGENT_RUNTIME_PORT:-9005}
# [SSOT] Redundant: AGENT_RUNTIME_MODEL=${AGENT_RUNTIME_MODEL:-gpt-4-turbo}
# [SSOT] Redundant: AGENT_RUNTIME_TIMEOUT=${AGENT_RUNTIME_TIMEOUT:-300}

# [SSOT] Redundant: EDGE_AGENT_TOKEN=${EDGE_AGENT_TOKEN:-dev-edge-agent-token-12345}
# [SSOT] Redundant: EDGE_AGENT_REMOTE_URL=${EDGE_AGENT_REMOTE_URL:-https://api.kushnir.cloud}

# ============================================================================
# SECTION 13: AUTHENTICATION & OAUTH CONFIGURATION
# ============================================================================
# [SSOT] Redundant: OAUTH_ENABLED=${OAUTH_ENABLED:-true}
# [SSOT] Redundant: OAUTH_GOOGLE_CLIENT_ID=${OAUTH_GOOGLE_CLIENT_ID:-dev-google-client-id}
# [SSOT] Redundant: OAUTH_GOOGLE_CLIENT_SECRET=${OAUTH_GOOGLE_CLIENT_SECRET:-dev-google-client-secret}
# [SSOT] Redundant: OAUTH_GITHUB_CLIENT_ID=${OAUTH_GITHUB_CLIENT_ID:-dev-github-client-id}
# [SSOT] Redundant: OAUTH_GITHUB_CLIENT_SECRET=${OAUTH_GITHUB_CLIENT_SECRET:-dev-github-client-secret}

# [SSOT] Redundant: OAUTH2_CLIENT_ID=${OAUTH2_CLIENT_ID:-dev-oauth2-client}
# [SSOT] Redundant: OAUTH2_CLIENT_SECRET=${OAUTH2_CLIENT_SECRET:-dev-oauth2-secret}
# [SSOT] Redundant: OAUTH2_COOKIE_SECRET=${OAUTH2_COOKIE_SECRET:-XW4vTbAaRob8vY&a9OAsEI2v}
# [SSOT] Redundant: OAUTH2_REDIRECT_URL=${OAUTH2_REDIRECT_URL:-http://kushnir.cloud/oauth2/callback}
# [SSOT] Redundant: OAUTH2_PROXY_COOKIE_SECRET=${OAUTH2_PROXY_COOKIE_SECRET:-dev-oauth2-proxy-cookie-secret-12345678901234}
# [SSOT] Redundant: OAUTH2_PROXY_PORT=${OAUTH2_PROXY_PORT:-4180}

# Code Server authentication
# [SSOT] Redundant: CODE_SERVER_PASSWORD=${CODE_SERVER_PASSWORD:-SecureCodeServerPassword2026!}
# [SSOT] Redundant: CODE_SERVER_PASSWORD_HASH=${CODE_SERVER_PASSWORD_HASH:-}

# ============================================================================
# SECTION 14: APPSMITH CONFIGURATION
# ============================================================================
# [SSOT] Redundant: APPSMITH_INSTANCE_NAME=${APPSMITH_INSTANCE_NAME:-kushnir-cloud-ide}
# [SSOT] Redundant: APPSMITH_ENCRYPTION_PASSWORD=${APPSMITH_ENCRYPTION_PASSWORD:-change-me-prod}
# [SSOT] Redundant: APPSMITH_ENCRYPTION_SALT=${APPSMITH_ENCRYPTION_SALT:-change-me-prod}
# [SSOT] Redundant: APPSMITH_INTERNAL_REDIS_URL=${APPSMITH_INTERNAL_REDIS_URL:-redis://redis-appsmith:6379}
# [SSOT] Redundant: APPSMITH_MONGODB_URI=${APPSMITH_MONGODB_URI:-mongodb://mongo-appsmith:27017/appsmith}

# ============================================================================
# SECTION 15: LOGGING & DEBUGGING CONFIGURATION
# ============================================================================
# [SSOT] Redundant: LOG_LEVEL=${LOG_LEVEL:-info}
# [SSOT] Redundant: LOG_FORMAT=${LOG_FORMAT:-json}
# [SSOT] Redundant: AUDIT_LOGGING_ENABLED=${AUDIT_LOGGING_ENABLED:-true}

# ============================================================================
# SECTION 16: API GATEWAY (CADDY) CONFIGURATION
# ============================================================================
# [SSOT] Redundant: CADDY_EMAIL=${CADDY_EMAIL:-admin@kushnir.cloud}
# [SSOT] Redundant: CADDY_AUTO_HTTPS=${CADDY_AUTO_HTTPS:-on}

# ============================================================================
# SECTION 17: EXTERNAL INTEGRATIONS
# ============================================================================
# [SSOT] Redundant: SLACK_SIGNING_SECRET=${SLACK_SIGNING_SECRET:-dev-slack-signing-secret-12345}
# [SSOT] Redundant: SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN:-dev-slack-bot-token-12345}

# [SSOT] Redundant: SENTRY_DSN=${SENTRY_DSN:-https://dev@sentry.example.com/123}
# [SSOT] Redundant: SENTRY_AUTH_TOKEN=${SENTRY_AUTH_TOKEN:-dev-sentry-token-12345}

# ============================================================================
# SECTION 18: STORAGE & NAS CONFIGURATION
# ============================================================================
# [SSOT] Redundant: NAS_HOST=${NAS_HOST:-192.168.168.56}
# [SSOT] Redundant: NAS_MOUNT_PATH=${NAS_MOUNT_PATH:-/mnt/nas}

# ============================================================================
# SECTION 19: DEPLOYMENT & FEATURE FLAGS
# ============================================================================
# [SSOT] Redundant: DEPLOYMENT_AUTO_ROLLBACK=${DEPLOYMENT_AUTO_ROLLBACK:-true}
# [SSOT] Redundant: DEPLOYMENT_ENV=${DEPLOYMENT_ENV:-production}
# [SSOT] Redundant: DOCKER_COMPOSE_VERSION=${DOCKER_COMPOSE_VERSION:-3.9}
# [SSOT] Redundant: ENABLE_METRICS=${ENABLE_METRICS:-true}

# ============================================================================
# SECTION 20: HEALTH CHECK CONFIGURATION
# ============================================================================
# [SSOT] Redundant: HEALTH_CHECK_TIMEOUT=${HEALTH_CHECK_TIMEOUT:-300}
# [SSOT] Redundant: HEALTH_CHECK_MAX_ATTEMPTS=${HEALTH_CHECK_MAX_ATTEMPTS:-30}
# [SSOT] Redundant: HEALTH_CHECK_INTERVAL=${HEALTH_CHECK_INTERVAL:-10}
