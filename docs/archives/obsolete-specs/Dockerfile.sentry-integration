FROM node:20.11.0-alpine

# Set working directory
WORKDIR /app

# Copy integration files
COPY scripts/integrations/sentry-integration-service.js ./
COPY scripts/integrations/sentry-integration-api.js ./
COPY scripts/integrations/sentry-error-analyzer.js ./
COPY package.json package-lock.json ./

# Install dependencies  
RUN npm ci --only=production

# Health check endpoint
HEALTHCHECK --interval=30s --timeout=5s --retries=3 --start-period=10s \
  CMD node -e "require('http').get('http://localhost:9095/health', (r) => r.statusCode === 200 ? process.exit(0) : process.exit(1))"

# Start API server
CMD ["node", "sentry-integration-api.js"]
