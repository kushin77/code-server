name: Policy SSOT Guard

on:
  pull_request:
    paths:
      - docs/governance/**
      - scripts/ci/check-policy-ssot.sh
      - scripts/ci/validate-policy-domain-registry.sh
      - scripts/ci/validate-epic-ac-overlap.sh
      - scripts/ci/validate-governance-issue-ac-text-overlap.sh
      - config/governance-policy-domains.json
      - config/governance-epic-ac-boundaries.json
      - .github/workflows/policy-ssot-guard.yml
  push:
    branches: [main]
    paths:
      - docs/governance/**
      - scripts/ci/check-policy-ssot.sh
      - scripts/ci/validate-policy-domain-registry.sh
      - scripts/ci/validate-epic-ac-overlap.sh
      - scripts/ci/validate-governance-issue-ac-text-overlap.sh
      - config/governance-policy-domains.json
      - config/governance-epic-ac-boundaries.json
      - .github/workflows/policy-ssot-guard.yml

permissions:
  contents: read
  issues: read
  pull-requests: write

jobs:
  policy-ssot-check:
    name: Policy SSOT duplicate and contradiction check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Install GitHub CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y gh

      - name: Run policy SSOT checker
        id: ssot
        continue-on-error: true
        run: |
          set +e
          bash scripts/ci/check-policy-ssot.sh policy-ssot-report.json
          rc=$?
          set -e
          echo "exit_code=$rc" >> "$GITHUB_OUTPUT"

      - name: Run policy domain registry validator
        id: domains
        continue-on-error: true
        run: |
          set +e
          bash scripts/ci/validate-policy-domain-registry.sh \
            config/governance-policy-domains.json \
            docs/governance/POLICY-INDEX.md \
            policy-domain-registry-report.json
          rc=$?
          set -e
          echo "exit_code=$rc" >> "$GITHUB_OUTPUT"

      - name: Run epic AC overlap validator
        id: overlap
        continue-on-error: true
        run: |
          set +e
          bash scripts/ci/validate-epic-ac-overlap.sh \
            config/governance-epic-ac-boundaries.json \
            epic-ac-overlap-report.json
          rc=$?
          set -e
          echo "exit_code=$rc" >> "$GITHUB_OUTPUT"

      - name: Run governance issue AC text overlap validator
        id: overlap_text
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set +e
          bash scripts/ci/validate-governance-issue-ac-text-overlap.sh \
            config/governance-epic-ac-boundaries.json \
            governance-issue-ac-text-overlap-report.json
          rc=$?
          set -e
          echo "exit_code=$rc" >> "$GITHUB_OUTPUT"

      - name: Upload SSOT report
        if: always()
        uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
        with:
          name: policy-ssot-report
          path: |
            policy-ssot-report.json
            policy-domain-registry-report.json
            epic-ac-overlap-report.json
            governance-issue-ac-text-overlap-report.json
          if-no-files-found: warn

      - name: Enforce changelog update for governance policy changes
        if: github.event_name == 'pull_request'
        run: |
          set -euo pipefail
          git fetch --no-tags --depth=1 origin "${{ github.base_ref }}"
          changed_governance_files=$(git diff --name-only "origin/${{ github.base_ref }}"...HEAD -- docs/governance | grep -v '^docs/governance/CHANGELOG.md$' || true)
          if [[ -n "$changed_governance_files" ]]; then
            if ! git diff --name-only "origin/${{ github.base_ref }}"...HEAD -- docs/governance/CHANGELOG.md | grep -q 'docs/governance/CHANGELOG.md'; then
              echo "Governance policy files changed without changelog update"
              echo "$changed_governance_files"
              exit 1
            fi
          fi

      - name: Enforce governance approval for policy changes
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git fetch --no-tags --depth=1 origin "${{ github.base_ref }}"
          changed_governance_files=$(git diff --name-only "origin/${{ github.base_ref }}"...HEAD -- docs/governance || true)
          if [[ -z "$changed_governance_files" ]]; then
            echo "No governance policy files changed; approval gate skipped"
            exit 0
          fi

          reviews_json=$(gh api "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/reviews")
          approvals=$(printf '%s' "$reviews_json" | jq '[.[] | select(.state == "APPROVED")] | length')

          if [[ "$approvals" -lt 1 ]]; then
            echo "Governance policy changes require at least one approved review"
            echo "Changed files:"
            echo "$changed_governance_files"
            exit 1
          fi

          echo "Governance approval gate satisfied with $approvals approval(s)"

      - name: Report summary
        if: always()
        run: |
          echo "## Policy SSOT Guard" >> "$GITHUB_STEP_SUMMARY"
          echo "- Checker exit code: ${{ steps.ssot.outputs.exit_code }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Domain registry exit code: ${{ steps.domains.outputs.exit_code }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Epic AC overlap exit code: ${{ steps.overlap.outputs.exit_code }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Epic AC text overlap exit code: ${{ steps.overlap_text.outputs.exit_code }}" >> "$GITHUB_STEP_SUMMARY"
          if [[ -f policy-ssot-report.json ]]; then
            echo "- Duplicate statements: $(jq -r '.summary.duplicate_statements' policy-ssot-report.json)" >> "$GITHUB_STEP_SUMMARY"
            echo "- Contradictions: $(jq -r '.summary.contradictions' policy-ssot-report.json)" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [[ -f policy-domain-registry-report.json ]]; then
            echo "- Domain registry errors: $(jq -r '.summary.error_count' policy-domain-registry-report.json)" >> "$GITHUB_STEP_SUMMARY"
            echo "- Domain registry warnings: $(jq -r '.summary.warning_count' policy-domain-registry-report.json)" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [[ -f epic-ac-overlap-report.json ]]; then
            echo "- AC overlap errors: $(jq -r '.summary.error_count' epic-ac-overlap-report.json)" >> "$GITHUB_STEP_SUMMARY"
            echo "- AC overlap warnings: $(jq -r '.summary.warning_count' epic-ac-overlap-report.json)" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [[ -f governance-issue-ac-text-overlap-report.json ]]; then
            echo "- AC text overlap errors: $(jq -r '.summary.overlap_error_count' governance-issue-ac-text-overlap-report.json)" >> "$GITHUB_STEP_SUMMARY"
            echo "- AC text overlap warnings: $(jq -r '.summary.overlap_warning_count' governance-issue-ac-text-overlap-report.json)" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Fail when SSOT violations exist
        run: |
          ssot_rc='${{ steps.ssot.outputs.exit_code }}'
          domains_rc='${{ steps.domains.outputs.exit_code }}'
          overlap_rc='${{ steps.overlap.outputs.exit_code }}'
          overlap_text_rc='${{ steps.overlap_text.outputs.exit_code }}'
          if [[ "$ssot_rc" != "0" || "$domains_rc" != "0" || "$overlap_rc" != "0" || "$overlap_text_rc" != "0" ]]; then
            echo "Policy SSOT violations detected"
            exit 1
          fi
