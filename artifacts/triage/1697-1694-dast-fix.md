## DAST remediation

Fixed the live production drift that caused `https://ide.kushnir.cloud/health` to return 404.

What changed:
- Synced the workspace `Caddyfile` to both replicas.
- Restarted `caddy` on `192.168.168.31` and `192.168.168.42`.
- Verified the public HTTPS health route now returns `200` on both replicas.
- Hardened `scripts/ops/validate-cluster-parity-all-replicas.sh` so it checks the public HTTPS health route per replica.

Verification:
- `curl -sk --resolve ide.kushnir.cloud:443:192.168.168.31 -o /dev/null -w "%{http_code}\n" https://ide.kushnir.cloud/health` -> `200`
- `curl -sk --resolve ide.kushnir.cloud:443:192.168.168.42 -o /dev/null -w "%{http_code}\n" https://ide.kushnir.cloud/health` -> `200`

This removes the `dast-target-unreachable` condition for the public health endpoint.