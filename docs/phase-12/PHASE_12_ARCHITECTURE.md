# Phase 12: Federated Architecture Deep Dive**Document**: Multi-site federation architecture with CRDT-based eventual consistency**Date**: April 13, 2026## Table of Contents1. [System Model](#system-model)2. [Multi-Region Topology](#multi-region-topology)3. [Data Replication Layer](#data-replication-layer)4. [CRDT Integration](#crdt-integration)5. [Conflict Resolution](#conflict-resolution)6. [Consistency Guarantees](#consistency-guarantees)7. [Failure Recovery](#failure-recovery)8. [Latency Analysis](#latency-analysis)## System Model### Design Principles1. **AP System** (Availability + Partition tolerance over strong Consistency)   - All regions remain operational during network partition   - Ensures users in any region can always write   - Eventual consistency within bounded time2. **CRDT-Based Merging**   - All application data represented as CRDTs   - Merge function is commutative, associative, idempotent   - No coordination needed for conflict resolution3. **Event-Driven Sync**   - Changes published as immutable events   - Events contain full operation context   - Replayed idempotently at all sites4. **Local-First Performance**   - Read/write latency = local cluster latency (< 50ms)   - No remote calls in critical path   - Async replication hidden from users### Formal Definition```System = {Sites, Data, Events, Merge}Sites = {US-East, EU-Central, APAC-Singapore, US-West}Data = {  Documents ∈ CRDT(Text),  Sessions ∈ CRDT(Set),  Metadata ∈ CRDT(Map)}Events = Operation[] where Operation = {  siteId: SiteID,  clock: VectorClock,  timestamp: UnixTimestamp,  payload: OperationPayload}Merge(s1, s2) = {  result where ∀op ∈ (s1.events ∪ s2.events):    insert(result, op) with proper CRDT semantics}```## Multi-Region Topology### Network Architecture```┌────────────────────────────────────────────────────────┐│                   Internet / Backbone                   ││                     (AWS Direct Connect)                │└────┬─────────┬──────────┬──────────┬─────────┬─────────┘     │         │          │          │         │   (50ms)   (120ms)    (150ms)   (180ms)   (200ms)     │         │          │          │         │┌────▼──┐  ┌───▼───┐  ┌───▼───┐  ┌──▼────┐  ┌──▼─────┐│US-East│  │EU-Cent│  │APAC-Sg│  │ US-W  │  │JP-Tokyo││Region │  │Region │  │Region │  │Region │  │Region  │└───┬──-┘  └───┬───┘  └───┬───┘  └──┬────┘  └──┬─────┘    │          │          │          │         │    └──────────┴──────────┴──────────┴─────────┘         Event Stream (Kafka)         Latency:  50-200ms```### Regional Clustering**Each Region**:- 3-5 Kubernetes nodes (HA cluster)- Local PostgreSQL primary- Local Redis cluster- code-server replicas (3+)- Event consumer (Kafka)**Cluster Characteristics**:- Fully autonomous (can operate disconnected)- Self-healing (Phase 11 HA)- Local strong consistency- Independent failure domains## Data Replication Layer### PostgreSQL Multi-Primary Replication**Technology**: PostgreSQL Logical Replication + Custom Merge```        US-East Primary (LSN: 1000/100)              ↙       ↘        LSN: 2000/100   LSN: 2000/100          ↙   ↘           ↙   ↘    EU-Central        APAC-Sg     (LSN: 2000)      (LSN: 2000)```**Replication Slots**:- One slot per remote region- Decouples replication progress- Automatic cleanup after 24 hours of inactivity- Conflict detection via insert triggers**Conflict Detection**:```sql-- Trigger on INSERT to detect conflictsCREATE TRIGGER detect_conflict_insertBEFORE INSERT ON documentsFOR EACH ROWEXECUTE FUNCTION detect_and_merge_conflict();FUNCTION detect_and_merge_conflict() {  existing = SELECT * WHERE id = NEW.id;  IF existing THEN    -- Merge via CRDT logic    NEW = merge(existing, NEW);  END IF;  RETURN NEW;}```### Redis Cluster Federation**Cross-Region Sync**:```Master in US-East → Cluster slot 0-5460  ↓ (SLAVEOF replication)Slave in APAC-Sg, EU-Central, US-WestConflict Resolution (Last-Write-Wins + Metadata):  timestamp + siteId determines winner```**Features**:- Read-your-writes within a region (local primary)- Eventual consistency across regions- TTL-aware expiration (synced)- Automatic failover to slave### Event Streaming Layer**Technology**: AWS Kinesis / Apache Kafka```┌──────────────────────────────────────────────┐│         Application Events Stream            │├──────────────────────────────────────────────┤│ document:create  document:edit  session:join ││ {siteId, clock,  timestamp,     payload}    │└──────────────────────────────────────────────┘           ↓ (100ms fan out)   ┌───────┼───────┬───────────┐   ↓       ↓       ↓           ↓  US    EU-Cent  APAC     US-West  Event Consumer - Applies idempotently        ↓   Local State Merge```**Guarantees**:- Ordering preserved (per shard by user)- Exactly-once delivery (idempotent ops)- Timeliness: 99th percentile < 100ms## CRDT Integration### Register (LWW - Last Write Wins)**Example: User Status**:```typescripttype Register<T> = {  value: T,  timestamp: UnixTimestamp,  siteId: SiteID}// Merge functionmerge(r1: Register<T>, r2: Register<T>): Register<T> {  return r1.timestamp > r2.timestamp    ? r1    : r2.timestamp > r1.timestamp    ? r2    : r1.siteId > r2.siteId ? r1 : r2  // Tiebreaker}```### Counter (PN-Counter - Positive/Negative)**Example: Analytics Counters**:```typescripttype PNCounter = {  positive: Map<SiteID, Number>,  // Increments  negative: Map<SiteID, Number>   // Decrements}// Merge: Element-wise maxmerge(c1, c2): PNCounter {  return {    positive: max_per_site(c1.positive, c2.positive),    negative: max_per_site(c1.negative, c2.negative)  }}// Total valuevalue(c): Number {  return sum(c.positive.values()) - sum(c.negative.values())}```### Text (RGA - Replicated Growable Array)**Example: Document Editing**:```typescripttype TextChar = {  id: UniqueID,           // (siteId, clock)  content: string,  visible: boolean,  tombstone: boolean}type TextCRDT = TextChar[]// Insert operationinsert(text, pos, content, id): TextCRDT {  // Insert with stable ID  // Preserves position relative to surrounding chars  // Handles concurrent inserts correctly}// Delete operationdelete(text, pos): TextCRDT {  // Mark as tombstone, don't remove  // Preserved for ordering}```### Set (OR-Set - Observed-Remove)**Example: Tag Collections**:```typescripttype TagSet = {  elements: Map<string, Set<UniqueID>>,  // Value → unique IDs  tombstones: Map<UniqueID, boolean>}// Add elementadd(set, value): TagSet {  id = (siteId, clock)  set.elements[value].add(id)}// Remove element (remove all IDs)remove(set, value): TagSet {  for id in set.elements[value]:    set.tombstones[id] = true}// Merge: Union of all visible elementsmerge(s1, s2): TagSet {  result.elements = union(s1.elements, s2.elements)  result.tombstones = union(s1.tombstones, s2.tombstones)}```## Conflict Resolution### Strategy Matrix| Data Type | Strategy | Latency | Correctness ||-----------|----------|---------|------------|| User profile | LWW (timestamp) | O(1) | Eventual state correct || Document | RGA (text CRDT) | O(doc size) | All edits preserved || Session | OR-Set | O(1) | Commutative membership || Counter | PN-Counter | O(1) | Strong consistency || Settings | LWW + merge | O(1) | Last preference wins |### Application-Level ResolutionFor operations requiring coordination:```typescript// Critical operation: Enforce at application levelasync function transfer(fromUser, toUser, amount) {  // 1. Read account states (may be stale)  from_balance = get(fromUser.balance)  // Local read  to_balance = get(toUser.balance)  // 2. Check invariants locally  if (from_balance < amount) {    throw InsufficientFunds  }  // 3. Perform operation  update(fromUser.balance, from_balance - amount)  update(toUser.balance, to_balance + amount)  // 4. Publish event (other sites may reject if conflict)  publish({    type: "transfer",    from: fromUser.id,    to: toUser.id,    amount: amount,    timestamp: now(),    siteId: local_site_id  })  // 5. Other sites: Accept if from still has funds  //    Reject if insufficient (creates correction event)}```## Consistency Guarantees### CAP Theorem Positioning```        Consistency (ACID)              △             /│\            / │ \           /  │  \          /   │   \    Strong         /    │    \   Consistency        /     │     \       /    ●╱│    ╲   Phase 11      /    / │ \    ╲  (Single Region)         /   │   \        /╱───●────╱\╲  Phase 12       /    Phase 12  ╲  (Multi-Region)      Partition        \ Availability      Tolerance        ╲```**Phase 12 Properties**:- **Strong consistency** within region (synchronous)- **Eventual consistency** across regions (<500ms, 99.5% < 100ms)- **Causal consistency** for user operations- **Read-your-writes** guaranteed### Happens-Before RelationAll operations ordered via vector clocks:```Event chain for document edit:  User A (US) adds "Hello"    clock: [US:1, EU:0, APAC:0]  User B (EU) adds "World"    clock: [US:1, EU:1, APAC:0]  User C (APAC) reads: "HelloWorld"    clock: [US:1, EU:1, APAC:0]```## Failure Recovery### Scenario 1: Single Site Outage**Failure**: EU-Central region unreachable for 2 hours```Timeline:  T=0:    EU connection lost  T=5min: Kinesis consumer paused (backlog grows)  T=1h:   EU recovers  T=1h:   Consumer catches up (replay 60min of events)  T=1h10: Full replication lag < 500ms```**Recovery**:- Event log retained for 24 hours (S3 backup)- Automatic replay when site recovers- No manual intervention needed### Scenario 2: Data Divergence**Detection**:```sql-- Periodic hash verification (every 5 min)SELECT md5_agg(content) FROM documents  WHERE updated_at > now() - interval '5 minutes'-- Compare across regionsmd5_us    = 'a1b2c3d4'md5_eu    = 'a1b2c3d4'  ✓ Matchmd5_apac  = 'e5f6g7h8'  ✗ Mismatch!```**Recovery**:```1. Identify divergent records2. Fetch full event log from all sites3. Replay in canonical order (vector clock)4. Converge to same state5. Publish reconciliation event```### Scenario 3: Complete Regional Partition**Failure**: All 3+ regions disconnected from each other```US cluster: Continues with local writesEU cluster: Continues with local writesAPAC cluster: Continues with local writesUsers see stale data but can write (local strong consistency)```**Recovery**:```1. Network restored2. Leaders elected per region (quorum)3. Event streams resynced4. CRDT merge applied5. Convergence reached within 100ms6. Causal chain verified```## Latency Analysis### Write Path Latency```User writes document:  1. Local validation:           1ms  2. Local DB write:             5ms  3. Local cache update:         1ms  4. Publish event to Kinesis:   5ms  ────────────────────────────────  TOTAL (critical path):        12ms  ← User sees thisAsync replication (parallel):  5. Kinesis propagates:        50-200ms (depends on region)  6. Remote consume + write:     10ms per region  7. Remote cache update:        1ms  ────────────────────────────────  TOTAL (non-critical):         60-220ms```**Key Insight**: User operations return in ~10ms (local cluster only), replication happens async in background### Read Path Latency (Strong)```User reads document (strong consistency required):  1. Route to primary region:    5ms (local) or 100-150ms (remote)  2. Read from primary DB:       5ms  ────────────────────────────────  TOTAL:                         5ms (local) or 105-155ms (remote)```### Read Path Latency (Eventual)```User reads document (eventual OK):  1. Route to local cluster:     0ms (already local)  2. Read from local cache:      1ms (if hit) or 5ms (cache miss)  ────────────────────────────────  TOTAL:                         1-5ms```## Monitoring & Alerting### Key Metrics```Replication Lag (all regions):  - Metric: max(remote.clock - local.clock) by region  - Alert: > 1000 for 5 minutes  - Critical: > 10000 (manual intervention)Event Queue Depth:  - Metric: kinesis.GetRecords().Records.length by shard  - Alert: > 100000 messages  - Critical: > 1000000Clock Synchronization:  - Metric: NTP offset between sites  - Alert: > 500ms  - Action: Trigger clock resyncData Divergence Hash:  - Metric: md5_hash(all_records) by region  - Alert: Hash mismatch for > 5 minutes  - Action: Trigger reconciliation```### Dashboard Template```Panel 1: Replication Lag (by region)  - US-East → EU: 45ms  - US-East → APAC: 62ms  - EU → APAC: 85msPanel 2: Event Processing Rate  - Kinesis put: 1000 events/s  - Consumer lag: < 100 messages  - Processing latency: 25msPanel 3: Regional Health  - US-East: ✓ Healthy  - EU-Central: ✓ Healthy  - APAC-Singapore: ✓ HealthyPanel 4: Data Divergence  - documents_table: ✓ In sync  - sessions_table: ✓ In sync  - analytics_table: ✓ In sync```---**Status**: Complete**Last Updated**: April 13, 2026

**Purpose**: Phase 12: Federated Architecture Deep Dive**Document**: Multi-site federation architecture with CRDT-based eventual consistency**Date**: April 13, 2026## Table of Contents1. [System Model](#system-model)2. [Multi-Region Topology](#multi-region-topology)3. [Data Replication Layer](#data-replication-layer)4. [CRDT Integration](#crdt-integration)5. [Conflict Resolution](#conflict-resolution)6. [Consistency Guarantees](#consistency-guarantees)7. [Failure Recovery](#failure-recovery)8. [Latency Analysis](#latency-analysis)## System Model### Design Principles1. **AP System** (Availability + Partition tolerance over strong Consistency)   - All regions remain operational during network partition   - Ensures users in any region can always write   - Eventual consistency within bounded time2. **CRDT-Based Merging**   - All application data represented as CRDTs   - Merge function is commutative, associative, idempotent   - No coordination needed for conflict resolution3. **Event-Driven Sync**   - Changes published as immutable events   - Events contain full operation context   - Replayed idempotently at all sites4. **Local-First Performance**   - Read/write latency = local cluster latency (< 50ms)   - No remote calls in critical path   - Async replication hidden from users### Formal Definition```System = {Sites, Data, Events, Merge}Sites = {US-East, EU-Central, APAC-Singapore, US-West}Data = {  Documents ∈ CRDT(Text),  Sessions ∈ CRDT(Set),  Metadata ∈ CRDT(Map)}Events = Operation[] where Operation = {  siteId: SiteID,  clock: VectorClock,  timestamp: UnixTimestamp,  payload: OperationPayload}Merge(s1, s2) = {  result where ∀op ∈ (s1.events ∪ s2.events):    insert(result, op) with proper CRDT semantics}```## Multi-Region Topology### Network Architecture```┌────────────────────────────────────────────────────────┐│                   Internet / Backbone                   ││                     (AWS Direct Connect)                │└────┬─────────┬──────────┬──────────┬─────────┬─────────┘     │         │          │          │         │   (50ms)   (120ms)    (150ms)   (180ms)   (200ms)     │         │          │          │         │┌────▼──┐  ┌───▼───┐  ┌───▼───┐  ┌──▼────┐  ┌──▼─────┐│US-East│  │EU-Cent│  │APAC-Sg│  │ US-W  │  │JP-Tokyo││Region │  │Region │  │Region │  │Region │  │Region  │└───┬──-┘  └───┬───┘  └───┬───┘  └──┬────┘  └──┬─────┘    │          │          │          │         │    └──────────┴──────────┴──────────┴─────────┘         Event Stream (Kafka)         Latency:  50-200ms```### Regional Clustering**Each Region**:- 3-5 Kubernetes nodes (HA cluster)- Local PostgreSQL primary- Local Redis cluster- code-server replicas (3+)- Event consumer (Kafka)**Cluster Characteristics**:- Fully autonomous (can operate disconnected)- Self-healing (Phase 11 HA)- Local strong consistency- Independent failure domains## Data Replication Layer### PostgreSQL Multi-Primary Replication**Technology**: PostgreSQL Logical Replication + Custom Merge```        US-East Primary (LSN: 1000/100)              ↙       ↘        LSN: 2000/100   LSN: 2000/100          ↙   ↘           ↙   ↘    EU-Central        APAC-Sg     (LSN: 2000)      (LSN: 2000)```**Replication Slots**:- One slot per remote region- Decouples replication progress- Automatic cleanup after 24 hours of inactivity- Conflict detection via insert triggers**Conflict Detection**:```sql-- Trigger on INSERT to detect conflictsCREATE TRIGGER detect_conflict_insertBEFORE INSERT ON documentsFOR EACH ROWEXECUTE FUNCTION detect_and_merge_conflict();FUNCTION detect_and_merge_conflict() {  existing = SELECT * WHERE id = NEW.id;  IF existing THEN    -- Merge via CRDT logic    NEW = merge(existing, NEW);  END IF;  RETURN NEW;}```### Redis Cluster Federation**Cross-Region Sync**:```Master in US-East → Cluster slot 0-5460  ↓ (SLAVEOF replication)Slave in APAC-Sg, EU-Central, US-WestConflict Resolution (Last-Write-Wins + Metadata):  timestamp + siteId determines winner```**Features**:- Read-your-writes within a region (local primary)- Eventual consistency across regions- TTL-aware expiration (synced)- Automatic failover to slave### Event Streaming Layer**Technology**: AWS Kinesis / Apache Kafka```┌──────────────────────────────────────────────┐│         Application Events Stream            │├──────────────────────────────────────────────┤│ document:create  document:edit  session:join ││ {siteId, clock,  timestamp,     payload}    │└──────────────────────────────────────────────┘           ↓ (100ms fan out)   ┌───────┼───────┬───────────┐   ↓       ↓       ↓           ↓  US    EU-Cent  APAC     US-West  Event Consumer - Applies idempotently        ↓   Local State Merge```**Guarantees**:- Ordering preserved (per shard by user)- Exactly-once delivery (idempotent ops)- Timeliness: 99th percentile < 100ms## CRDT Integration### Register (LWW - Last Write Wins)**Example: User Status**:```typescripttype Register<T> = {  value: T,  timestamp: UnixTimestamp,  siteId: SiteID}// Merge functionmerge(r1: Register<T>, r2: Register<T>): Register<T> {  return r1.timestamp > r2.timestamp    ? r1    : r2.timestamp > r1.timestamp    ? r2    : r1.siteId > r2.siteId ? r1 : r2  // Tiebreaker}```### Counter (PN-Counter - Positive/Negative)**Example: Analytics Counters**:```typescripttype PNCounter = {  positive: Map<SiteID, Number>,  // Increments  negative: Map<SiteID, Number>   // Decrements}// Merge: Element-wise maxmerge(c1, c2): PNCounter {  return {    positive: max_per_site(c1.positive, c2.positive),    negative: max_per_site(c1.negative, c2.negative)  }}// Total valuevalue(c): Number {  return sum(c.positive.values()) - sum(c.negative.values())}```### Text (RGA - Replicated Growable Array)**Example: Document Editing**:```typescripttype TextChar = {  id: UniqueID,           // (siteId, clock)  content: string,  visible: boolean,  tombstone: boolean}type TextCRDT = TextChar[]// Insert operationinsert(text, pos, content, id): TextCRDT {  // Insert with stable ID  // Preserves position relative to surrounding chars  // Handles concurrent inserts correctly}// Delete operationdelete(text, pos): TextCRDT {  // Mark as tombstone, don't remove  // Preserved for ordering}```### Set (OR-Set - Observed-Remove)**Example: Tag Collections**:```typescripttype TagSet = {  elements: Map<string, Set<UniqueID>>,  // Value → unique IDs  tombstones: Map<UniqueID, boolean>}// Add elementadd(set, value): TagSet {  id = (siteId, clock)  set.elements[value].add(id)}// Remove element (remove all IDs)remove(set, value): TagSet {  for id in set.elements[value]:    set.tombstones[id] = true}// Merge: Union of all visible elementsmerge(s1, s2): TagSet {  result.elements = union(s1.elements, s2.elements)  result.tombstones = union(s1.tombstones, s2.tombstones)}```## Conflict Resolution### Strategy Matrix| Data Type | Strategy | Latency | Correctness ||-----------|----------|---------|------------|| User profile | LWW (timestamp) | O(1) | Eventual state correct || Document | RGA (text CRDT) | O(doc size) | All edits preserved || Session | OR-Set | O(1) | Commutative membership || Counter | PN-Counter | O(1) | Strong consistency || Settings | LWW + merge | O(1) | Last preference wins |### Application-Level ResolutionFor operations requiring coordination:```typescript// Critical operation: Enforce at application levelasync function transfer(fromUser, toUser, amount) {  // 1. Read account states (may be stale)  from_balance = get(fromUser.balance)  // Local read  to_balance = get(toUser.balance)  // 2. Check invariants locally  if (from_balance < amount) {    throw InsufficientFunds  }  // 3. Perform operation  update(fromUser.balance, from_balance - amount)  update(toUser.balance, to_balance + amount)  // 4. Publish event (other sites may reject if conflict)  publish({    type: "transfer",    from: fromUser.id,    to: toUser.id,    amount: amount,    timestamp: now(),    siteId: local_site_id  })  // 5. Other sites: Accept if from still has funds  //    Reject if insufficient (creates correction event)}```## Consistency Guarantees### CAP Theorem Positioning```        Consistency (ACID)              △             /│\            / │ \           /  │  \          /   │   \    Strong         /    │    \   Consistency        /     │     \       /    ●╱│    ╲   Phase 11      /    / │ \    ╲  (Single Region)         /   │   \        /╱───●────╱\╲  Phase 12       /    Phase 12  ╲  (Multi-Region)      Partition        \ Availability      Tolerance        ╲```**Phase 12 Properties**:- **Strong consistency** within region (synchronous)- **Eventual consistency** across regions (<500ms, 99.5% < 100ms)- **Causal consistency** for user operations- **Read-your-writes** guaranteed### Happens-Before RelationAll operations ordered via vector clocks:```Event chain for document edit:  User A (US) adds "Hello"    clock: [US:1, EU:0, APAC:0]  User B (EU) adds "World"    clock: [US:1, EU:1, APAC:0]  User C (APAC) reads: "HelloWorld"    clock: [US:1, EU:1, APAC:0]```## Failure Recovery### Scenario 1: Single Site Outage**Failure**: EU-Central region unreachable for 2 hours```Timeline:  T=0:    EU connection lost  T=5min: Kinesis consumer paused (backlog grows)  T=1h:   EU recovers  T=1h:   Consumer catches up (replay 60min of events)  T=1h10: Full replication lag < 500ms```**Recovery**:- Event log retained for 24 hours (S3 backup)- Automatic replay when site recovers- No manual intervention needed### Scenario 2: Data Divergence**Detection**:```sql-- Periodic hash verification (every 5 min)SELECT md5_agg(content) FROM documents  WHERE updated_at > now() - interval '5 minutes'-- Compare across regionsmd5_us    = 'a1b2c3d4'md5_eu    = 'a1b2c3d4'  ✓ Matchmd5_apac  = 'e5f6g7h8'  ✗ Mismatch!```**Recovery**:```1. Identify divergent records2. Fetch full event log from all sites3. Replay in canonical order (vector clock)4. Converge to same state5. Publish reconciliation event```### Scenario 3: Complete Regional Partition**Failure**: All 3+ regions disconnected from each other```US cluster: Continues with local writesEU cluster: Continues with local writesAPAC cluster: Continues with local writesUsers see stale data but can write (local strong consistency)```**Recovery**:```1. Network restored2. Leaders elected per region (quorum)3. Event streams resynced4. CRDT merge applied5. Convergence reached within 100ms6. Causal chain verified```## Latency Analysis### Write Path Latency```User writes document:  1. Local validation:           1ms  2. Local DB write:             5ms  3. Local cache update:         1ms  4. Publish event to Kinesis:   5ms  ────────────────────────────────  TOTAL (critical path):        12ms  ← User sees thisAsync replication (parallel):  5. Kinesis propagates:        50-200ms (depends on region)  6. Remote consume + write:     10ms per region  7. Remote cache update:        1ms  ────────────────────────────────  TOTAL (non-critical):         60-220ms```**Key Insight**: User operations return in ~10ms (local cluster only), replication happens async in background### Read Path Latency (Strong)```User reads document (strong consistency required):  1. Route to primary region:    5ms (local) or 100-150ms (remote)  2. Read from primary DB:       5ms  ────────────────────────────────  TOTAL:                         5ms (local) or 105-155ms (remote)```### Read Path Latency (Eventual)```User reads document (eventual OK):  1. Route to local cluster:     0ms (already local)  2. Read from local cache:      1ms (if hit) or 5ms (cache miss)  ────────────────────────────────  TOTAL:                         1-5ms```## Monitoring & Alerting### Key Metrics```Replication Lag (all regions):  - Metric: max(remote.clock - local.clock) by region  - Alert: > 1000 for 5 minutes  - Critical: > 10000 (manual intervention)Event Queue Depth:  - Metric: kinesis.GetRecords().Records.length by shard  - Alert: > 100000 messages  - Critical: > 1000000Clock Synchronization:  - Metric: NTP offset between sites  - Alert: > 500ms  - Action: Trigger clock resyncData Divergence Hash:  - Metric: md5_hash(all_records) by region  - Alert: Hash mismatch for > 5 minutes  - Action: Trigger reconciliation```### Dashboard Template```Panel 1: Replication Lag (by region)  - US-East → EU: 45ms  - US-East → APAC: 62ms  - EU → APAC: 85msPanel 2: Event Processing Rate  - Kinesis put: 1000 events/s  - Consumer lag: < 100 messages  - Processing latency: 25msPanel 3: Regional Health  - US-East: ✓ Healthy  - EU-Central: ✓ Healthy  - APAC-Singapore: ✓ HealthyPanel 4: Data Divergence  - documents_table: ✓ In sync  - sessions_table: ✓ In sync  - analytics_table: ✓ In sync```---**Status**: Complete**Last Updated**: April 13, 2026 — reference and operational document.
