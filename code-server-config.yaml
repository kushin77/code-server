# code-server Configuration
# Deploy to: ~/.config/code-server/config.yaml
#
# NOTE: auth is disabled here because authentication is handled by oauth2-proxy (SSO).
# Caddy → oauth2-proxy (Google OAuth) → code-server (auth: none)
# All password prompts are removed at the infrastructure level.

bind-addr: 0.0.0.0:8080
auth: none
cert: false

# Security: trusted proxy domains for port-forwarding tabs
proxy-domain:
  - ide.kushnir.cloud
  - kushnir.cloud
  - localhost
  - 127.0.0.1

# Logging for audit trail
logging: debug

# Extensions marketplace
# NOTE: Disabled marketplace validation since Copilot Chat is pre-installed via VSIX
# and not available on Open VSX. Code-server will use cached VSIX files from entrypoint.
# extension-gallery: https://open-vsx.org/vscode/gallery

# Disable telemetry for compliance
disable-telemetry: true

# Resource limits per session
max-connections: 100

# Session timeout (30 minutes)
session-timeout: 1800000
