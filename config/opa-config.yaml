services:
  localhost:
    url: http://opa:8181

bundles:
  policies:
    service: localhost
    resource: /bundles/policies.tar.gz
    polling:
      min_delay_seconds: 60
      max_delay_seconds: 300

decision_logs:
  console:
    level: info

