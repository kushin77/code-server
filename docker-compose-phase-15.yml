version: '3.9'

# Phase 15: Advanced Performance & Observability
# Note: Phase 15 uses existing Phase 14 Redis instance (localhost:6379)
# No separate Redis instance deployed - avoiding duplicate containers

services:
  # Monitoring and observability services only
  # Phase 14 Redis (stable, tested) is reused at redis:6379
  
  # Placeholder for future Phase 15 services
  # (Advanced observability features, load testing harness, etc.)
  noop:
    image: busybox:latest
    entrypoint: /bin/sh -c "echo 'Phase 15 uses Phase 14 Redis (localhost:6379)' && sleep 1"
    restart: "no"
