################################################################################\n# ENVIRONMENT VARIABLES & TEMPLATES CONSOLIDATION GUIDE\n# Purpose: Single source of truth for all environment configuration\n# Status: Production-ready reference implementation\n################################################################################\n\n# ═══════════════════════════════════════════════════════════════════════════════\n# PART 1: CANONICAL ENVIRONMENT VARIABLES (Master List)\n# ═══════════════════════════════════════════════════════════════════════════════\n\n# Location: .env (NEVER COMMIT - git ignored)\n# Template: .env.example (commit to repo)\n# Override: environment-specific overrides in docker-compose.override.yml\n\n### APPLICATION CONFIGURATION\nNODE_ENV=production\nSERVICE_NAME=code-server-enterprise\nSERVICE_VERSION=1.0.0\nTZ=UTC\n\n### DEPLOYMENT CONFIGURATION\nDEPLOYMENT_HOST=192.168.168.31\nDEPLOYMENT_ENV=production\nSTANDBY_HOST=192.168.168.42\nNAS_HOST=192.168.168.56\nNAS_PATH=/exports\n\n### DATABASE CONFIGURATION\nDB_HOST=localhost\nDB_PORT=5432\nDB_NAME=code_server\nDB_USER=codeserver\nDB_PASS=${SECRET_DB_PASSWORD}  # From GSM, not hardcoded\nDB_POOL_MIN=5\nDB_POOL_MAX=20\nDB_POOL_IDLE_TIMEOUT=30000\nDB_SSL=require\n\n### CACHE CONFIGURATION\nREDIS_HOST=localhost\nREDIS_PORT=6379\nREDIS_PASSWORD=${SECRET_REDIS_PASSWORD}  # From GSM\nREDIS_DB=0\nREDIS_TTL=3600\n\n### AUTHENTICATION & SECRETS\nOAUTH2_CLIENT_ID=${SECRET_OAUTH2_CLIENT_ID}\nOAUTH2_CLIENT_SECRET=${SECRET_OAUTH2_CLIENT_SECRET}\nOAUTH2_REDIRECT_URI=https://code-server.${DOMAIN}/auth/callback\nJWT_SECRET=${SECRET_JWT_SECRET}\nAUTH_COOKIE_SECURE=true\nAUTH_COOKIE_HTTPONLY=true\nAUTH_COOKIE_SAMESITE=Strict\n\n### SECURITY CONFIGURATION\nCSRF_TOKEN_SECRET=${SECRET_CSRF_TOKEN}\nENCRYPTION_KEY=${SECRET_ENCRYPTION_KEY}\nREQUEST_SIGNING_KEY=${SECRET_REQUEST_SIGNING_KEY}\nAUDIT_LOG_SECRET=${SECRET_AUDIT_LOG_SECRET}\n\n### GPU CONFIGURATION\nCUDA_VISIBLE_DEVICES=1\nOLLAMA_NUM_GPU=1\nOLLAMA_MAX_MEMORY=8g\nTF_FORCE_GPU_ALLOW_GROWTH=true\n\n### OBSERVABILITY CONFIGURATION\nPROMETHEUS_PORT=9090\nGRAFANA_PORT=3000\nGRAFANA_ADMIN_PASSWORD=${SECRET_GRAFANA_ADMIN_PASSWORD}\nALERTMANAGER_PORT=9093\nJAEGER_AGENT_HOST=localhost\nJAEGER_AGENT_PORT=6831\nLOG_LEVEL=info\n\n### NAS & BACKUP CONFIGURATION\nNAS_BACKUP_ENABLED=true\nNAS_BACKUP_INTERVAL=3600\nNAS_MOUNT_OPTIONS=soft,timeo=100,retrans=3,noresvport\nBACKUP_RETENTION_DAYS=30\n\n### VPN & NETWORKING\nVPN_GATEWAY_IP=192.168.168.1\nVPN_ENDPOINT_PORT=1194\nVPN_PROTOCOL=udp\nVPN_TLS_AUTH_KEY=${SECRET_VPN_TLS_KEY}\nVPN_CIPHER=AES-256-GCM\n\n### FEATURE FLAGS\nFEATURE_READONLY_IDE=true\nFEATURE_GIT_PROXY=true\nFEATURE_DATABASE_OPTIMIZATION=true\nFEATURE_CANARY_DEPLOYMENT=false  # Set to true for gradual rollouts\nCANARY_ROLLOUT_PERCENTAGE=1  # Start at 1%, increase gradually\n\n### RATE LIMITING\nRATE_LIMIT_WINDOW_MS=60000\nRATE_LIMIT_MAX_REQUESTS=100\nRATE_LIMIT_BY_IP=true\n\n### HEALTH CHECK CONFIGURATION\nHEALTH_CHECK_LIVENESS_INTERVAL=30s\nHEALTH_CHECK_READINESS_INTERVAL=30s\nHEALTH_CHECK_START_PERIOD=40s\nHEALTH_CHECK_TIMEOUT=10s\nHEALTH_CHECK_RETRIES=3\n\n# ═══════════════════════════════════════════════════════════════════════════════\n# PART 2: ENVIRONMENT-SPECIFIC TEMPLATES\n# ═══════════════════════════════════════════════════════════════════════════════\n\n### TEMPLATE 1: .env.example (Commit to repo)\n# Copy to .env for local development\n# DO NOT COMMIT .env\n# All SECRET_* values must be injected via GSM or environment\n\nNODE_ENV=development\nDEPLOYMENT_HOST=localhost\nDEPLOYMENT_ENV=development\nDB_HOST=localhost\nDB_USER=codeserver\nDB_PASS=REPLACE_WITH_SECURE_PASSWORD\nREDIS_PASSWORD=REPLACE_WITH_SECURE_PASSWORD\nOAUTH2_CLIENT_ID=REPLACE_WITH_OAUTH2_CLIENT_ID\nOAUTH2_CLIENT_SECRET=REPLACE_WITH_SECRET\nJWT_SECRET=REPLACE_WITH_SECRET\nLOG_LEVEL=debug\n\n### TEMPLATE 2: docker-compose.override.yml (Development overrides)\n# Use this to override production config locally without committing\nversion: '3.8'\nservices:\n  code-server:\n    environment:\n      NODE_ENV: development\n      LOG_LEVEL: debug\n      DB_HOST: postgres  # Use local container\n      REDIS_HOST: redis  # Use local container\n      FEATURE_CANARY_DEPLOYMENT: 'false'\n  postgres:\n    environment:\n      POSTGRES_PASSWORD: dev-password  # OK for local dev\n\n### TEMPLATE 3: .env.production (For production host via GSM)\n# Never commit this - generated at deployment time from Terraform/GSM\nNODE_ENV=production\nDEPLOYMENT_HOST=192.168.168.31\nDEPLOYMENT_ENV=production\nDB_HOST=postgres\nDB_USER=codeserver\nDB_PASS=${DATABASE_PASSWORD_GSM}  # Injected from GSM\nREDIS_PASSWORD=${REDIS_PASSWORD_GSM}\nOAUTH2_CLIENT_ID=${OAUTH2_CLIENT_ID_GSM}\nOAUTH2_CLIENT_SECRET=${OAUTH2_CLIENT_SECRET_GSM}\nJWT_SECRET=${JWT_SECRET_GSM}\nFEATURE_CANARY_DEPLOYMENT=true\nCANARY_ROLLOUT_PERCENTAGE=1\nLOG_LEVEL=info\n\n### TEMPLATE 4: .env.staging (For staging/standby host)\nNODE_ENV=staging\nDEPLOYMENT_HOST=192.168.168.42\nDEPLOYMENT_ENV=staging\nDB_HOST=postgres\nDB_USER=codeserver\nDB_PASS=${DATABASE_PASSWORD_GSM_STAGING}\nREDIS_PASSWORD=${REDIS_PASSWORD_GSM_STAGING}\nFEATURE_CANARY_DEPLOYMENT=true\nCANARY_ROLLOUT_PERCENTAGE=10  # Test with 10% rollout\nLOG_LEVEL=debug\n\n# ═══════════════════════════════════════════════════════════════════════════════\n# PART 3: TERRAFORM VARIABLES CONSOLIDATION\n# ═══════════════════════════════════════════════════════════════════════════════\n\n# File: terraform/variables.tf\n# Single source of truth for all input variables\n\nvariable \"deployment_host\" {\n  description = \"Primary production host IP\"\n  type        = string\n  default     = \"192.168.168.31\"\n}\n\nvariable \"standby_host\" {\n  description = \"Standby/replica host IP\"\n  type        = string\n  default     = \"192.168.168.42\"\n}\n\nvariable \"nas_host\" {\n  description = \"NAS storage host IP\"\n  type        = string\n  default     = \"192.168.168.56\"\n}\n\nvariable \"environment\" {\n  description = \"Deployment environment\"\n  type        = string\n  default     = \"production\"\n  validation {\n    condition     = contains([\"development\", \"staging\", \"production\"], var.environment)\n    error_message = \"Environment must be development, staging, or production\"\n  }\n}\n\nvariable \"container_image_versions\" {\n  description = \"Pinned container image versions (never use :latest)\"\n  type        = map(string)\n  default = {\n    code_server  = \"codercom/code-server:4.115.0\"\n    oauth2_proxy = \"quay.io/oauth2-proxy/oauth2-proxy:v7.5.1\"\n    caddy        = \"caddy:2.7.6-alpine\"\n    postgres     = \"postgres:15.2-alpine\"\n    redis        = \"redis:7.0-alpine\"\n    prometheus   = \"prom/prometheus:v2.48.0\"\n    grafana      = \"grafana/grafana:10.2.3\"\n    alertmanager = \"prom/alertmanager:v0.26.0\"\n  }\n}\n\nvariable \"resource_limits\" {\n  description = \"Container resource limits\"\n  type        = map(object({ memory_limit = string, memory_reservation = string, cpu_limit = string }))\n  default = {\n    code_server = { memory_limit = \"4g\", memory_reservation = \"512m\", cpu_limit = \"2.0\" }\n    postgres    = { memory_limit = \"2g\", memory_reservation = \"256m\", cpu_limit = \"1.0\" }\n    redis       = { memory_limit = \"768m\", memory_reservation = \"64m\", cpu_limit = \"0.5\" }\n  }\n}\n\nvariable \"health_check_config\" {\n  description = \"Health check configuration\"\n  type        = map(number)\n  default = {\n    start_period_seconds = 40\n    interval_seconds     = 30\n    timeout_seconds      = 10\n    retries              = 3\n  }\n}\n\n# ═══════════════════════════════════════════════════════════════════════════════\n# PART 4: DOCKER-COMPOSE TEMPLATE VARIABLES\n# ═══════════════════════════════════════════════════════════════════════════════\n\n# File: docker-compose.yml\n# Uses environment variable substitution\n# Reference in docker-compose.yml as: ${VARIABLE_NAME:default_value}\n\nservices:\n  code-server:\n    image: ${CODE_SERVER_IMAGE:-codercom/code-server:4.115.0}\n    environment:\n      - NODE_ENV=${NODE_ENV:-production}\n      - SERVICE_NAME=${SERVICE_NAME:-code-server-enterprise}\n      - LOG_LEVEL=${LOG_LEVEL:-info}\n      - DB_HOST=${DB_HOST:-localhost}\n      - DB_PORT=${DB_PORT:-5432}\n      - REDIS_HOST=${REDIS_HOST:-localhost}\n      - REDIS_PORT=${REDIS_PORT:-6379}\n      - JWT_SECRET=${JWT_SECRET:?err}\n    ports:\n      - \"${CODE_SERVER_PORT:-8080}:8080\"\n    healthcheck:\n      test: [\"CMD-SHELL\", \"curl -sf http://localhost:8080/health/ready\"]\n      interval: ${HEALTH_CHECK_INTERVAL:-30}s\n      timeout: ${HEALTH_CHECK_TIMEOUT:-10}s\n      retries: ${HEALTH_CHECK_RETRIES:-3}\n      start_period: ${HEALTH_CHECK_START_PERIOD:-40}s\n\n# ═══════════════════════════════════════════════════════════════════════════════\n# PART 5: SECRETS MANAGEMENT (GSM Integration)\n# ═══════════════════════════════════════════════════════════════════════════════\n\n# File: scripts/load-secrets-from-gsm.sh\n# Load all secrets from Google Secret Manager at runtime\n\n#!/bin/bash\n# Load database password from GSM\nexport DB_PASS=$(gcloud secrets versions access latest --secret=\"db-password-prod\")\n\n# Load Redis password from GSM\nexport REDIS_PASSWORD=$(gcloud secrets versions access latest --secret=\"redis-password-prod\")\n\n# Load OAuth2 credentials from GSM\nexport OAUTH2_CLIENT_ID=$(gcloud secrets versions access latest --secret=\"oauth2-client-id\")\nexport OAUTH2_CLIENT_SECRET=$(gcloud secrets versions access latest --secret=\"oauth2-client-secret\")\n\n# Load JWT secret from GSM\nexport JWT_SECRET=$(gcloud secrets versions access latest --secret=\"jwt-secret-prod\")\n\n# Load encryption keys from GSM\nexport ENCRYPTION_KEY=$(gcloud secrets versions access latest --secret=\"encryption-key-prod\")\nexport REQUEST_SIGNING_KEY=$(gcloud secrets versions access latest --secret=\"request-signing-key\")\n\n# Launch application with injected secrets\ndocker-compose up -d\n\n# ═══════════════════════════════════════════════════════════════════════════════\n# PART 6: USAGE & BEST PRACTICES\n# ═══════════════════════════════════════════════════════════════════════════════\n\nBEST PRACTICES:\n\n1. LOCAL DEVELOPMENT (.env)\n   - Copy .env.example to .env\n   - Modify for local environment\n   - Never commit .env to git\n   - .gitignore includes .env pattern\n\n2. PRODUCTION DEPLOYMENT\n   - Secrets loaded from GSM (no .env file)\n   - docker-compose.yml uses ${VAR:default} syntax\n   - Override via environment before docker-compose up\n   - Use scripts/load-secrets-from-gsm.sh\n\n3. STAGING/TESTING\n   - Use docker-compose.override.yml for local dev\n   - Use .env.staging for test environments\n   - Feature flags for gradual rollout testing\n\n4. VARIABLE NAMING\n   - Secrets: SECRET_* (never hardcode)\n   - Config: UPPERCASE_WITH_UNDERSCORES\n   - Feature flags: FEATURE_* (boolean)\n   - Defaults in docker-compose: ${VAR:default}\n\n5. DOCUMENTATION\n   - All variables documented in this file\n   - Comments explain purpose of each variable\n   - Example values provided in templates\n   - Validation rules in terraform/variables.tf\n\nVALIDATION CHECKLIST:\n- [ ] All ${SECRET_*} variables loaded from GSM\n- [ ] No hardcoded credentials in any file\n- [ ] .env files not in git history\n- [ ] docker-compose.override.yml for local development\n- [ ] Terraform variables validated and scoped\n- [ ] Environment-specific configs separated\n- [ ] Feature flags operational\n- [ ] Health check timeouts appropriate for environment\n"