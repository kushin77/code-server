{
  "name": "agent-farm",
  "displayName": "Agent Farm - Multi-Agent Development System",
  "description": "Specialized agents for code analysis, review, architecture, and testing",
  "version": "0.1.0",
  "publisher": "kushin77",
  "repository": {
    "type": "git",
    "url": "https://github.com/kushin77/code-server.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Debuggers",
    "Testing",
    "Linters"
  ],
  "keywords": [
    "ai",
    "agent",
    "copilot",
    "code-review",
    "refactoring",
    "testing"
  ],
  "icon": "resources/icon.png",
  "activationEvents": [
    "onStartupFinished",
    "onCommand:agent-farm.analyzeFile",
    "onCommand:agent-farm.analyzeWithTask",
    "onView:agentFarmView"
  ],
  "main": "./out/index.js",
  "contributes": {
    "commands": [
      {
        "command": "agent-farm.executeTask",
        "title": "Agent Farm: Execute Task",
        "description": "Route task to specialized agents"
      },
      {
        "command": "agent-farm.showDashboard",
        "title": "Agent Farm: Show Dashboard",
        "description": "Display agent farm status and history"
      },
      {
        "command": "agent-farm.semanticSearch",
        "title": "Agent Farm: Semantic Search",
        "description": "Search code by meaning"
      },
      {
        "command": "agent-farm.analyzeFile",
        "title": "Agent Farm: Analyze File",
        "description": "Run agents on current file"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "agent-farm-dashboard",
          "name": "Agent Farm",
          "when": "workspaceFolderCount != 0"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "agent-farm-container",
          "title": "Agent Farm",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "configuration": {
      "title": "Agent Farm",
      "properties": {
        "agentFarm.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Agent Farm"
        },
        "agentFarm.agents": {
          "type": "array",
          "default": [
            "code",
            "review"
          ],
          "description": "Enabled agents (code, review, architect, test)"
        },
        "agentFarm.maxConcurrentAgents": {
          "type": "number",
          "default": 2,
          "description": "Maximum concurrent agent executions"
        },
        "agentFarm.auditTrail": {
          "type": "boolean",
          "default": true,
          "description": "Enable audit trail logging"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "tsc --noEmit",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --coverageReporters=text-lcov",
    "pretest": "npm run compile"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  }
}
