# Phase 2 Additional Configuration
# ⚠️  TEMPLATE ONLY - Secrets loaded from Google Secret Manager

# Database credentials
DB_PASSWORD=${DB_PASSWORD:?Error: DB_PASSWORD not loaded from GSM}
DB_REPLICATION_PASSWORD=${DB_REPLICATION_PASSWORD:?Error: DB_REPLICATION_PASSWORD not loaded from GSM}

# TLS certificates and keys
TLS_CERT_PATH=/etc/certs/tls.crt
TLS_KEY_PATH=/etc/certs/tls.key
# Keys should be mounted from secret volume, not in .env

# Redis
REDIS_PASSWORD=${REDIS_PASSWORD:?Error: REDIS_PASSWORD not loaded from GSM}

# OAuth tokens
GITHUB_TOKEN=${GITHUB_TOKEN:?Error: GITHUB_TOKEN not loaded from GSM}

# Signing keys
JWT_SIGNING_KEY=${JWT_SIGNING_KEY:?Error: JWT_SIGNING_KEY not loaded from GSM}
