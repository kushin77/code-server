global
  maxconn 4096
  log /dev/log local0 info
  log /dev/log local0 notice
  stats socket /run/haproxy/admin.sock mode 660 level admin
  stats timeout 30s
  user haproxy
  group haproxy
  daemon
  
  # Default SSL material locations
  ca-base /etc/ssl/certs
  crt-base /etc/ssl/private

  # SSL configuration
  ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
  ssl-default-bind-options no-sslv3 no-tlsv10 no-tlsv11 no-tlsv12 no-ticket
  ssl-default-server-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
  ssl-default-server-options no-sslv3 no-tlsv10 no-tlsv11 no-tlsv12 no-ticket

defaults
  log global
  mode http
  option httplog
  option dontlognull
  timeout connect 5s
  timeout client 50s
  timeout server 50s
  
  # Error pages
  errorfile 400 /usr/local/etc/haproxy/errors/400.http
  errorfile 403 /usr/local/etc/haproxy/errors/403.http
  errorfile 408 /usr/local/etc/haproxy/errors/408.http
  errorfile 500 /usr/local/etc/haproxy/errors/500.http
  errorfile 502 /usr/local/etc/haproxy/errors/502.http
  errorfile 503 /usr/local/etc/haproxy/errors/503.http
  errorfile 504 /usr/local/etc/haproxy/errors/504.http

frontend main_lb
  bind *:80
  # bind *:443 ssl crt /etc/haproxy/certs/ide.pem
  
  # Health check for external status
  monitor-uri /healthz
  
  # Stats endpoint
  stats enable
  stats uri /haproxy-stats
  stats refresh 30s
  stats auth admin:admin123 # Should be secured via env/secret in deploy stage
  
  # Session persistence
  stick-table type string len 32 size 100k expire 30m
  stick on cookie(JSESSIONID)
  
  default_backend code_server_backend

backend code_server_backend
  balance roundrobin
  option httpchk GET /healthz HTTP/1.1 Host:\ ide.kushnir.cloud
  http-check expect status 200,307,401
  
  # Primary node (192.168.168.31)
  server primary 192.168.168.31:8080 check port 8080 inter 10s fall 3 rise 2 weight 100
  
  # Replica node (192.168.168.42) - Standby
  server replica 192.168.168.42:8080 check port 8080 inter 10s fall 3 rise 2 backup
  
  # Custom logging
  log-format "%ci:%cp [%tr] %ft %b/%s %TR/%Tw/%Tc/%Tr/%Ta %ST %B %CC %CS %tsc %ac/%fc/%bc/%sc/%rc %sq/%bq {%[cookie_value(JSESSIONID)]} %{-Q}r"
