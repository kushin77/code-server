# vrrp.yaml — PLAINTEXT TEMPLATE (do NOT commit with real values)
# ================================================================
# Populate with real VRRP auth password, then encrypt:
#   sops -e secrets/vrrp.yaml > secrets/vrrp.enc.yaml
#   rm secrets/vrrp.yaml  # Never commit plaintext
#
# Use with deploy-keepalived.sh:
#   sops exec-env secrets/vrrp.enc.yaml 'bash scripts/deploy-keepalived.sh'

keepalived:
  vrrp_auth_pass: "REPLACE_WITH_32_CHAR_HEX_openssl_rand_hex_16"
